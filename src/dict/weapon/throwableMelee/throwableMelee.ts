import throwing_knife from "@src/dict/weapon/throwableMelee/knife/throwing_knife";
import { WeaponDictEntity } from "@src/dict/weapon/weapon";

export const throwable_melee: { [id: string]: WeaponDictEntity } = {
  throwing_knife,
};

export default throwable_melee;
