import broken_bottle from "@src/dict/weapon/melee/broken_bottle";
import { WeaponDictEntity } from "@src/dict/weapon/weapon";

export const melee: { [id: string]: WeaponDictEntity } = {
  broken_bottle: broken_bottle,
};

export default melee;
